# Python bytecode and import caches
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/
env/

# Tool caches
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Large local dataset cache (Common Voice parquet shards; extract reads ../cv_cache/en)
cv_cache/

# Per-utterance feature table — ~101 MB, exceeds GitHub's 100 MB limit.
# Regenerate locally with `python extract_v2.py`.
features.parquet